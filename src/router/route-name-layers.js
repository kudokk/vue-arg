import { ROUTE_NAMES } from '@/router/names.js'

export const ROUTE_NAME_LAYERS = Object.freeze({
  // トップの階層に属するルート名
  TOP_LAYER: [
    ROUTE_NAMES.CAMPAIGNS,
    ROUTE_NAMES.STRUCTS,
    ROUTE_NAMES.ADS
  ],
  // すべてのキャンペーンの階層に属するルート名
  CAMPAIGNS_LAYER: [
    ROUTE_NAMES.CAMPAIGNS,
    ROUTE_NAMES.CAMPAIGN_CREATE
  ],
  // すべてのストラクトの階層に属するルート名
  STRUCTS_LAYER: [
    ROUTE_NAMES.STRUCTS,
    ROUTE_NAMES.STRUCT_CREATE
  ],
  // すべての広告の階層に属するルート名
  ADS_LAYER: [
    ROUTE_NAMES.ADS,
    ROUTE_NAMES.AD_CREATE,
    ROUTE_NAMES.AD_DETAIL
  ],
  // 特定キャンペーンの階層に属するルート名
  CAMPAIGN_LAYER: [
    ROUTE_NAMES.CAMPAIGN_STRUCTS,
    ROUTE_NAMES.CAMPAIGN_ADS,
    ROUTE_NAMES.CAMPAIGN_STRUCT_CREATE,
    ROUTE_NAMES.CAMPAIGN_AD_CREATE,
    ROUTE_NAMES.CAMPAIGN_DETAIL,
    ROUTE_NAMES.CAMPAIGN_AD_DETAIL
  ],
  // 特定ストラクトの階層に属するルート名
  STRUCT_LAYER: [
    ROUTE_NAMES.STRUCT_ADS,
    ROUTE_NAMES.STRUCT_AD_CREATE,
    ROUTE_NAMES.STRUCT_DETAIL,
    ROUTE_NAMES.CAMPAIGN_STRUCT_DETAIL,
    ROUTE_NAMES.STRUCT_AD_DETAIL
  ],
  PLACEMENT_TARGETINGS_LAYER: [
    ROUTE_NAMES.PLACEMENT_TARGETINGS,
    ROUTE_NAMES.PLACEMENT_TARGETING_CREATE,
    ROUTE_NAMES.PLACEMENT_TARGETING_DETAIL
  ]
})
